#include <iostream>

int main()
{
    int Numero = 0;
    while << (Numero <=50)
    {
        if (Numero % 2 == 0)
        {
            std::cout << Numero << " ";
        }
    //De alguma forma voce precisa incrementar a variavel numero para que ela 
    //chegue ate 50
    //Qual condicao de parada de loop?
    Numero++;
    }
    system("PAUSE");
    return 0;

}
